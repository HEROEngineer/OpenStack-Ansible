- name: Copy glance-api.conf to /etc/glance/
  template:
    src: glance-api.conf.j2
    dest: /etc/glance/glance-api.conf

- name: Copy glance-registry.conf to /etc/glance/
  template:
    src: glance-registry.conf.j2
    dest: /etc/glance/glance-registry.conf
