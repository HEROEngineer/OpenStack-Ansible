- name: Install glance
  apt:
    name: glance

- name: Prerequisites Database
  include: mysql.yml

- name: Copy glance.conf
  include: config.yml

- name: Restart the Glance service
  service:
    name: "{{ item }}"
    state: restarted
  with_items:
    - glance-registry
    - glance-api
